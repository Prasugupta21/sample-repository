class Solution {
public:
    int jump(vector<int>& nums) {
    int i=0;
    int farthest=0,curr=0,jumps=0;
    for(;i<nums.size()-1;i++){
        farthest=max(farthest,i+nums[i]);
        if(i==curr){
            curr=farthest;
            jumps++;

        }
    }
        return jumps;
    }
};